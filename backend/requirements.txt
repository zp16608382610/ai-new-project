fastapi==0.141.1
uvicorn==0.52.4
pydantic-settings==2.15.0
sqlalchemy==2.0.52
alembic==1.19.2
psycopg[binary]==3.3.5
mcp==2.1.1
# OpenAI-compatible HTTP transport for the DeepSeek provider (Phase 7B).
# Used only to call the remote chat/completions API; not an Agent framework.
httpx==0.28.1
